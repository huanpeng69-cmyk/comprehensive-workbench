# Docker Compose 变量模板。复制为 .env（已 gitignore）后修改。
# docker compose 在 server/ 目录运行时自动读取本文件。
WORKBENCH_DB_USER=workbench
# PostgreSQL 密码仅在首次建库时生效；已有数据卷需用 ALTER USER 修改
WORKBENCH_DB_PASSWORD=workbench123
# Redis 访问密码，必须与 server/config/application.yml 中的 redis 密码一致
WORKBENCH_REDIS_PASSWORD=<替换为随机值>
