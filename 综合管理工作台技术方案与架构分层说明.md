# 综合管理工作台技术方案与架构分层说明

- **文档版本**：V1.0
- **日期**：2026-09-18
- **推荐路线**：模块化单体 + 分层架构 + 可插拔 AI 适配层
- **适用阶段**：MVP 至中期迭代

---

## 1. 结论先行

不建议一开始做微服务、复杂消息中台或自研工资核算引擎。推荐采用：

> **前后端分离的模块化单体应用，后端按领域模块组织，内部采用接口层—应用层—领域层—基础设施层—数据层分层；外部系统通过集成层接入；大模型通过独立 AI Gateway/Adapter 接入。**

这套方案的优点：

- 初期部署简单、成本低、开发效率高；
- 人力、办公室、党建、工会边界清晰，后续可独立拆分；
- 工资和敏感数据可以单独做权限、审计和数据库访问控制；
- 写作台可以先不用 AI，后续平滑接入不同模型；
- 不把大模型能力直接写死在业务模块中，降低供应商绑定风险。

---

## 2. 总体架构图（带分层注释）

~~~text
┌──────────────────────────────────────────────────────────────┐
│                    访问与展现层                              │
│  PC Web / 移动 H5 / 企业微信或钉钉入口                         │
│  注释：负责页面、表单、列表、图表、编辑器；不承载核心业务规则。 │
└───────────────────────────┬──────────────────────────────────┘
                            │ HTTPS / REST / SSE
┌───────────────────────────▼──────────────────────────────────┐
│                    接口与接入层                              │
│  API Controller / BFF / 统一认证 / 参数校验 / 限流 / 防重提交   │
│  注释：把外部请求转换成内部用例；统一处理身份、权限和错误格式。 │
└───────────────────────────┬──────────────────────────────────┘
                            │ Command / Query
┌───────────────────────────▼──────────────────────────────────┐
│                    应用服务层                                │
│  待办、流程、工资批次、写作任务、活动报名、会议预约等用例服务  │
│  注释：编排业务步骤、事务边界和权限校验；不直接写 SQL。        │
└───────────────────────────┬──────────────────────────────────┘
                            │ Domain Service / Event
┌───────────────────────────▼──────────────────────────────────┐
│                    领域业务层                                │
│  人力 / 工资 / 办公室 / 写作台 / 党建 / 工会 / 协同中心          │
│  注释：沉淀业务实体、状态流转、规则和领域约束；保持模块内聚。   │
└───────────────────────────┬──────────────────────────────────┘
                            │ Repository / Port
┌───────────────────────────▼──────────────────────────────────┐
│                    基础设施层                                │
│  权限、消息、文件、搜索、报表、任务调度、审计、AI 适配层        │
│  注释：实现通用技术能力和外部依赖；通过接口隔离具体供应商。      │
└───────────────────────────┬──────────────────────────────────┘
                            │ ORM / SQL / Object Storage
┌───────────────────────────▼──────────────────────────────────┐
│                    数据与外部系统层                          │
│  PostgreSQL / Redis / 对象存储 / 搜索引擎（按需）                │
│  HR、薪资、财务、OA、统一身份认证、企业微信/钉钉、模型供应商     │
│  注释：明确谁是权威来源；工作台不复制不必要的专业数据。         │
└──────────────────────────────────────────────────────────────┘
~~~

---

## 3. 推荐技术栈

### 3.1 前端

| 领域 | 推荐 | 说明 |
|---|---|---|
| Web 框架 | Vue 3 + TypeScript | 上手快、适合管理后台和表单系统 |
| 构建工具 | Vite | 配置简单、开发反馈快 |
| UI 组件 | Element Plus | 表格、表单、审批、弹窗等后台场景成熟 |
| 状态管理 | Pinia | 管理用户、权限、待办、编辑器状态 |
| 图表 | ECharts | 适合管理驾驶舱和统计报表 |
| 写作编辑器 | Tiptap 或同类 ProseMirror 编辑器 | 支持协同扩展、批注、格式化和导出 |
| 移动端 | 优先 H5/响应式，后续接企业微信或钉钉 | 避免一开始维护原生 App |

### 3.2 后端

| 领域 | 推荐 | 说明 |
|---|---|---|
| 语言/框架 | Java + Spring Boot | 企业内部系统生态成熟，权限、事务和集成能力强 |
| ORM | MyBatis-Plus 或 Spring Data JPA | 团队熟悉哪一种优先；复杂报表可保留 SQL |
| 接口 | REST 为主，SSE 用于消息/进度推送 | 足够支持 MVP，避免过早引入复杂 RPC |
| 定时任务 | Quartz 或应用内任务调度 | 工资期间提醒、合同到期、逾期催办 |
| 权限 | Spring Security + RBAC/数据权限 | 支持组织、角色、字段和操作级权限 |
| 文件处理 | MinIO/兼容对象存储 | 附件、材料、工资条和归档文件统一管理 |

### 3.3 数据与基础设施

| 领域 | 推荐 | 说明 |
|---|---|---|
| 关系数据库 | PostgreSQL | 事务、JSON、全文能力和扩展性较平衡 |
| 缓存 | Redis | 会话、验证码、权限缓存、幂等键、短期任务状态 |
| 搜索 | MVP 先用 PostgreSQL 查询；数据量变大再引入 OpenSearch/Elasticsearch | 避免早期增加运维组件 |
| 消息 | MVP 先用数据库任务表；确有异步量后再引入 RabbitMQ/Kafka | 通知、导出、AI 任务可异步化 |
| 部署 | Docker Compose 起步，后续视规模迁移 Kubernetes | 小规模组织不必一开始上 K8s |
| 监控 | 日志 + 健康检查 + 指标监控 | 至少覆盖接口、任务、数据库、文件和模型调用 |

### 3.4 大模型能力

| 组件 | 推荐做法 | 作用 |
|---|---|---|
| AI Gateway | 独立后端模块/服务 | 统一模型调用、超时、重试、计费和审计 |
| Model Adapter | OpenAI-compatible、国产模型、私有模型分别实现适配器 | 更换模型不影响写作台业务代码 |
| Prompt Registry | 数据库存储版本化提示词模板 | 可审查、回滚、A/B 测试 |
| Knowledge Retrieval | 先做权限过滤的制度/素材检索，再接向量检索 | 防止模型引用无权限材料 |
| AI Audit | 保存请求用户、材料范围、模型、提示词版本、输出和反馈 | 便于追责、优化和敏感数据审计 |
| Safety Policy | 敏感材料禁用、脱敏、长度限制、人工确认 | 防止把工资、党员、帮扶等信息无控制地发送给模型 |

---

## 4. 分层职责与编码约束

### 4.1 访问与展现层

**负责：** 页面、路由、组件、表单校验、列表展示、状态提示、编辑体验。

**不负责：** 工资计算、审批路径判断、组织数据权限判断、归档规则。

**示例：** 写作台页面可以显示“生成摘要”按钮，但不能在前端拼接模型 API Key，也不能自行决定哪些材料可以发送给模型。

### 4.2 接口与接入层

**负责：** 登录态解析、请求参数校验、幂等键、统一错误码、分页、接口版本、权限入口检查。

**不负责：** 直接操作数据库，不在 Controller 中编写完整业务流程。

**建议：** 统一返回 requestId，便于定位工资导入、材料导出和 AI 请求问题。

### 4.3 应用服务层

**负责：** 一个完整业务用例的编排，例如“发布工资条”“提交写作审核”“完成会议任务”。

**关键规则：**

- 明确事务边界；
- 调用领域对象完成状态变化；
- 调用仓储接口读写数据；
- 调用消息、文件、AI 等端口，但不依赖具体厂商 SDK；
- 处理重试、幂等和跨模块协作。

### 4.4 领域业务层

按业务边界拆分为：

~~~text
modules/
├─ hr/                  # 组织、人员、人力事项
├─ project/             # 项目部、成立与撤并、人员归属
├─ payroll/             # 工资期间、工资批次、工资条、异议
├─ employment/          # 临时工、外聘、返聘台账
├─ office/              # 公告、会议、用印、行政事务
├─ document/            # 公文模板、文号、签发、收发文
├─ reception/           # 公务接待三联单、A/B/C 分类审批
├─ seal/                # 用章申请、公章使用登记
├─ vehicle/             # 派车、行车记录、单车核算
├─ canteen/             # 食堂补贴、客餐、餐费清单
├─ duty/                # 值班排班（领导/人员/司机）
├─ rectification/       # 审计整改与自查任务台账
├─ writing/             # 写作空间、模板、版本、批注、审核
├─ party/               # 党组织、党员、组织生活、学习
├─ partyfee/            # 党费基数、季度明细、封顶规则
├─ partycost/           # 党组织活动经费使用审批
├─ union/               # 会员、活动、福利、帮扶、提案
├─ workflow/            # 流程定义、实例、节点、待办
├─ notification/        # 站内信、回执、消息模板
├─ identity/            # 用户、组织、角色、数据范围
└─ audit/               # 操作日志、敏感访问、导出审计
~~~

每个模块内部建议保持：

~~~text
api/                    # 接口 DTO、Controller
application/             # 用例服务、Command、Query
domain/                 # 实体、值对象、领域服务、规则
infrastructure/          # Repository 实现、第三方适配
~~~

**模块依赖规则：**

- payroll 可以读取 identity 的人员/组织引用，但不复制完整人员主数据；
- 所有业务模块按 project（项目部）做数据隔离，跨项目部查询需显式授权；
- employment 的三类台账字段集不同，禁止合并成一张宽表；
- writing 可以调用 workflow 发起审核，但不直接修改 workflow 表；
- party 和 union 复用人员主数据，但分别维护党员/会员业务属性；
- 所有模块通过应用服务或事件协作，禁止跨模块直接访问对方数据库表。

### 4.5 基础设施层

**负责：** 数据库、缓存、对象存储、邮件/短信/企业微信、搜索、定时任务、导出、AI 适配器。

**核心原则：** 业务层依赖抽象接口，基础设施层依赖具体实现。

基础设施层额外沉淀四个通用引擎：

- **表单引擎**：工资项目、食堂补贴项目、党团用品品类等“列可配置”的台账，用动态字段方案实现，避免每加一列就改表结构；
- **文档引擎**：红头公文模板（上行文/下行文/会议纪要）、文号自动生成（类别+年度+序号，保证不重号不跳号）、人民币大写转换、24 小时异议期计算；
- **排班引擎**：节日值班与日常值班的按日轮班、司机排班、防暑费（6-10 月）等周期性补贴的日历触发；
- **分类路由**：审批引擎支持按业务分类（如接待 A/B/C）自动选择审批人，以及跨部门会签（制表—人资—财务—主要负责人）。

例如：

~~~text
写作台应用服务
    ↓ 依赖
TextGenerationPort
    ↓ 实现
OpenAICompatibleAdapter / PrivateModelAdapter / MockAdapter
~~~

这样在本地测试时可以使用 MockAdapter，生产环境再配置具体模型。

### 4.6 数据层

**推荐：** 先使用一个 PostgreSQL 实例，按模块使用表前缀或数据库 schema 做逻辑隔离；不要一开始拆成多个数据库。

所有业务表默认带 `project_id`（项目部）字段并建立索引；查询层强制注入项目部过滤，公司级角色通过显式授权跨项目查看。人员表按用工形式（正式/临时/外聘/返聘）区分字段集。

**必须单独加强控制的表：**

- 工资明细、工资条、工资异议；
- 身份证件、联系方式等人员敏感信息；
- 党员和组织关系敏感字段；
- 困难帮扶和家庭信息；
- 写作台敏感材料、审计日志。

**数据写入原则：**

- 所有关键业务表包含创建人、创建时间、更新时间、版本号；
- 关键状态变化记录事件或操作日志；
- 工资批次和定稿材料采用版本化，不覆盖历史版本；
- 导入和导出都保存批次、操作者、数据范围和结果摘要。

---

## 5. 工资模块的特殊架构处理

~~~text
薪资/考勤/社保专业系统
          │  同步工资计算结果或批次状态
          ▼
┌─────────────────────────┐
│ 工作台 Payroll Adapter  │  ← 适配不同 HR/薪资系统
└───────────┬─────────────┘
            ▼
┌─────────────────────────┐
│ 工资批次与审核应用服务   │  ← 审核、发布、异议、归档
└───────────┬─────────────┘
            ▼
┌─────────────────────────┐
│ 工资条查询与权限策略     │  ← 员工只看本人
└─────────────────────────┘
~~~

建议先支持 Excel 模板导入和人工核对，再做接口同步。工资模块上线前必须补充：

- 工资口径确认；
- 计算来源和批次唯一标识；
- 复核人与发布人分离；
- 工资条脱敏和下载审计；
- 更正、补发、重发和历史版本处理；
- 数据备份与恢复演练。

---

## 6. 写作台与大模型的演进路线

### 阶段 1：无 AI 的写作协同

模板、编辑、评论、版本、审核、定稿、导出、归档先跑通。

### 阶段 2：低风险 AI 辅助

优先上线：

- 标题/提纲建议；
- 摘要生成；
- 错别字和格式检查；
- 语气/长度改写；
- 会议纪要初稿；
- 任务项提取。

所有结果必须以“建议稿”形式返回，由用户主动采纳，不覆盖原文。

### 阶段 3：权限感知的知识问答

- 制度和历史材料建立检索索引；
- 检索前执行用户权限过滤；
- 输出附引用材料和来源；
- 未找到依据时明确提示“没有检索到可靠依据”；
- 对敏感材料设置不可检索或仅指定角色可检索。

### 阶段 4：流程智能化

- 根据材料类型推荐模板和审核流程；
- 自动识别缺少的附件、数据和落款；
- 识别文中任务并生成待办草稿；
- 分析逾期风险，但不替代人工审批。
- 会议纪要初稿：由签到表、议题和记录自动整理，保留 24 小时异议期提示；
- 自查报告初稿：按专项模板（农民工工资、劳务派遣、公务用车等）生成检查项与自评表；
- 整改台账摘要：汇总问题、任务、证据与销号状态；
- 公文格式与文号校对：检查主送/抄送/附件/落款是否齐全，文号是否重复；
- 制度检索：公务用车管理办法、薪酬分配办法、工会经费收支管理办法等，检索前做权限过滤。

---

## 7. 部署建议

### MVP 部署

~~~text
Nginx
  ├─ 前端静态文件
  └─ 后端模块化单体
        ├─ PostgreSQL
        ├─ Redis
        └─ MinIO
~~~

- 可使用一台应用服务器 + 一台数据库/存储服务器起步；
- 生产环境至少做数据库备份、对象存储备份和日志留存；
- AI 调用可先由后端统一代理，不在浏览器直连模型供应商；
- 不建议把工资明细放入前端缓存或第三方分析工具。

### 中期扩展

当出现以下情况再拆服务：

- AI 任务量明显影响主业务；
- 文件转换/导出成为独立性能瓶颈；
- 消息通知需要独立扩容；
- 某个模块有完全不同的发布周期或安全域；
- 组织规模和并发量已通过压测证明单体需要拆分。

优先拆分顺序建议：AI/异步任务 → 文件与导出 → 消息中心 → 搜索 → 业务模块，不要反过来一开始就拆四大模块。

---

## 8. 研发实施顺序

1. 组织、用户、角色、数据权限；
2. 统一流程、待办、消息和审计；
3. 人员主数据与人力事项；
4. 工资批次、工资条、审核和异议；
5. 公告、会议和办公室行政事项；
6. 写作台模板、编辑、评论、版本和审核；
7. 党建和工会核心台账；
8. 报表、导出和移动端适配；
9. AI Gateway、模型适配和低风险写作辅助；
10. 权限感知的知识检索和智能流程建议。

---

## 9. 架构验收清单

- [ ] 前端不能绕过后端直接访问数据库或模型供应商；
- [ ] Controller 不包含核心业务规则和 SQL；
- [ ] 每个模块有明确边界和唯一数据责任人；
- [ ] 工资、党员、帮扶、敏感材料有独立数据权限策略；
- [ ] 流程、待办、消息和审计可被四大模块复用；
- [ ] 工资批次、工资条和定稿材料支持版本追踪；
- [ ] 写作台 AI 功能通过适配器接入，业务代码不绑定具体 SDK；
- [ ] AI 处理前有权限校验、敏感标记和审计记录；
- [ ] 关键导入、导出、发布、审批操作具备幂等和日志；
- [ ] MVP 可以在单体部署下运行，后续拆分不改变业务接口。

---

## 10. 方案取舍

| 方案 | 结论 | 原因 |
|---|---|---|
| 微服务起步 | 不推荐 | 运维、部署、链路追踪和数据一致性成本过高 |
| 全部功能做在前端 | 不推荐 | 权限、工资和流程规则无法安全控制 |
| 自研完整工资核算 | 暂不推荐 | 政策和规则复杂，风险高，已有专业系统可复用 |
| 直接把模型 SDK 写进写作台 | 不推荐 | 供应商绑定、敏感数据控制和测试困难 |
| 模块化单体 + 分层 + AI 适配层 | 推荐 | 简单可落地，同时保留后续扩展空间 |

---

## 11. 架构决策记录

### ADR-T001：MVP 采用模块化单体，不采用微服务

- **背景**：初期功能跨模块但用户规模、并发和团队规模尚未明确。
- **决策**：单体部署，代码按领域模块和分层组织；通过接口和事件保持可拆分性。
- **原因**：降低交付和运维复杂度，先验证业务价值；只有性能、组织或安全边界明确后再拆分。

### ADR-T002：大模型通过 AI Gateway 和适配器接入

- **背景**：未来可能使用云端模型、兼容 OpenAI API 的模型或私有化模型。
- **决策**：业务模块只依赖统一的文本生成、摘要、检索和校对接口，具体模型由适配器实现。
- **原因**：实现模型可替换、调用可审计、敏感材料可拦截、测试可模拟。
